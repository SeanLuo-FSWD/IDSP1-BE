import { Request, Response, NextFunction, Router } from 'express';




class APIRouter {
    public router = Router();
    public path = "/api";
    constructor() {
        
    }

    initAPIRoute() {
        this.
    }
}